{% extends 'base.html.twig' %}

{% block title %}Reclamations{% endblock %}

{% block body %}
    <h1>Reclamations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ reclamation.sujet }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ reclamation.dateCreation }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reclamation.status }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ reclamation.message }}</td>
            </tr>
            <tr>
                <th>Captureecranpath</th>
                <td>{{ reclamation.captureecranpath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamations_index') }}">back to list</a>

    <a href="{{ path('app_reclamations_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('reclamations/_delete_form.html.twig') }}
{% endblock %}

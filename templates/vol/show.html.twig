{% extends 'base.html.twig' %}

{% block title %}Books!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
<h1>la liste des livres</h1>
    
    <table border="1">
        <thead>
            <tr>
                <th>Ville de destination</th>
                <th>Date de depart</th>
                <th>Date d ' arrivée</th>
                <th>action</th>
            </tr>
        </thead>
        <tbody>
            {% for vol in vols %}
                <tr>
                    <td>{{ vol.villeDestination }}</td>
                    <td>{{ vol.DatDeDepart|date('Y-m-d') }}</td>
                    <td>{{ vol.DateDarrivee|date('Y-m-d') }}</td>
                    <td><a href="{{ path('app_reservation_new', {'id': vol.id}) }}">reserver</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    
</div>

{% endblock %}